import {useEffect,useState} from "react";import "./Navigation.css";import {business,navItems} from "../../utils/constants.js";
function Navigation(){const[open,setOpen]=useState(false);useEffect(()=>{document.body.classList.toggle("nav-open",open);return()=>document.body.classList.remove("nav-open")},[open]);return <nav className="navigation" aria-label="Primary navigation"><button className="navigation__toggle" type="button" aria-expanded={open} aria-controls="primary-menu" aria-label={open?"Close menu":"Open menu"} onClick={()=>setOpen(!open)}><span/><span/><span/></button><div id="primary-menu" className={`navigation__panel ${open?"navigation__panel--open":""}`}><ul>{navItems.map(item=><li key={item.href}><a href={item.href} onClick={()=>setOpen(false)}>{item.label}</a></li>)}</ul><a className="navigation__mobile-call button button--yellow" href={business.phoneHref}>Call {business.phoneDisplay}</a></div></nav>}export default Navigation;
